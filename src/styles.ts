import { Grid } from "@hudoro/neron";
import styled from "styled-components";

export const TopNavWrapper = styled(Grid)`
  width: calc(100% - 200px);
`;

export const ContentWrapper = styled(Grid)`
  padding: 20px;
  width: 100%;
  min-height: 100vh;
  display: block;
`;
